export type ProjectType = {
  name: string;
  description: string;
  image: string;
  link: string;
};

export type ProjectDetailledType = {
  id: string;
  name: string;
  description: string;
  image: string;
  technologies: string[];
  github: string;
};
