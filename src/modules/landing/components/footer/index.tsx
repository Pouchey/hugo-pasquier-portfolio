import Reveal from '_components/reveal';

import './style.css';

export default () => {
  return (
    <div className="landing-footer">
      <Reveal type="fadeInUp" threshold={0.5}>
        fdfsdf
      </Reveal>
    </div>
  );
};
